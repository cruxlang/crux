fun manhattan(p) {
    p.x + p.y
}

fun main() {
    let a: {x: Number, y: Number} = {x:44, y:0}
    print(manhattan(a))

    let b: {mutable x: Number, mutable y: Number} = {x:0, y:0}
    print(manhattan(b))
}
