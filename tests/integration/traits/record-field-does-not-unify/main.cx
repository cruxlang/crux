import dict

trait PD {
  pd: self => ()
}

impl PD {...} {
  for fieldValue { fieldValue + 1 }
  pd(r) {
    let d = dict.from(fieldMap(r))
    for item in sorted(d->items()) {
      let (key, value) = item
      print(key)
      print(value)
    }
  }
}

fun main() {
  pd({x: "hello"})
}
