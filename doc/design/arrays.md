Specifying array type:
Array a

Literal syntax:
[a0, a1, a2, ...]

Arrays are mutable.

len(s) to calculate the length.
s->push(1)
s->pop()

Go has sugar: []a
TypeScript has sugar: a[]
Haskell has sugar: [a]
Punt on sugar for now.
