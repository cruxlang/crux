pragma { NoBuiltin }

import {
  boolean
}

export trait Eq a {
  eq: fun(a, a) -> boolean.Boolean
  neq: fun(a, a) -> boolean.Boolean
}

impl Eq boolean.Boolean {
  eq = _unsafe_js("function eq(x, y) { return x === y; }")
  neq = _unsafe_js("function neq(x, y) { return x !== y; }")
}
