trait Trait a {
  method: fun(a, a) -> a
  display: fun(a) -> String
}

impl Trait Number {
  method(x, y) { x + y }
  display(z) { toString(z) }
}

fun main(x, y) {
  print(display(method(x, y)))
}

let _ = (_debug_type(main); main(10, 5))
