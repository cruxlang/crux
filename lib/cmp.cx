pragma { NoBuiltin }

import {
  boolean
}

export trait Eq {
  eq(self, self): boolean.Boolean
}

export fun neq(lhs, rhs) {
  boolean.not(lhs == rhs)
}

impl Eq boolean.Boolean {
  eq = _unsafe_js("function eq(x, y) { return x === y; }")
}
