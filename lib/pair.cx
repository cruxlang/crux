pragma { NoBuiltin }

import {
  cmp
}

// TODO: generalize tuples
export data Pair<a, b>(a, b)

impl cmp.Eq Pair<a: cmp.Eq, b: cmp.Eq> {
  eq = fun(Pair(a, b), Pair(c, d)) {
    a == c && b == d
  }
}
