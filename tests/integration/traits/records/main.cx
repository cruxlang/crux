trait GetType {
  getType: self => String
}

impl GetType Number {
  getType = _ => "number"
}

impl GetType String {
  getType = _ => "string"
}

/*
impl GetType {...} {
  for fieldValue { () }
  getType(d: {...: ()}) { return "record" }
}
*/

/*
impl json.ToJSON {...} {
  for fieldValue {
    return json.toJSON(fieldValue)
  }
  toJSON(d: {...: json.Value}) {
    return json.toJSON(dict.from(d))
  }
}
*/

fun main() {
  print(getType(10))
  print(getType("hello"))
  //print(getType({x: 10}))
}
