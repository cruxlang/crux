pragma { NoBuiltin }

import {
  boolean(...)
  cmp(...)
  pair
}

export data Option<a> {
  Some(a),
  None,
}

impl Eq Option<a: Eq> {
  eq(lhs, rhs) {
    match pair.Pair(lhs, rhs) {
      pair.Pair(None, None) => True
      pair.Pair(Some(x), Some(y)) => x == y
      _ => False
    }
  }
}
