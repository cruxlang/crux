import test(...)
import assert

let _ = test("number min and max", fun() {
  assert.equal(1, min(1, 2))
  assert.equal(2, max(1, 2))
})

let _ = test("number compare", fun() {
  assert.equal(LessThan, compare(1, 2))
  assert.equal(GreaterThan, compare(2, 1))
  assert.equal(EqualTo, compare(1, 1))
})
