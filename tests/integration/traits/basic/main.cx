trait Trait a {
  method: fun(a, a) -> a
  display: fun(a) -> String
}

impl Trait Number {
  method = fun(x, y) { x + y }
  display = fun(z) { toString(z) }
}

fun main() {
  print(display(method(10, 5)))
}

let _ = main()
