# Writing a New npm Module in Crux

```
mkdir npmtest
crux init
crux build
```

## Obvious Improvements

* `crux init` should automatically create a skeleton project
* add support for `export jsffi` to avoid accidentally exporting things that depend on traits
