pragma { NoBuiltin }

import number(Number)

// TODO: return an iterator
export let range: Number => [Number] = _unsafe_js(
    "function(n) local a = {} for i=0,n do table.insert(a, i) end return a end"
)
