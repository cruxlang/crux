pragma { NoBuiltin }

import types(...)
import cmp(...)

export data Option<a> {
  None,
  Some(a),
}

impl Eq Option<a: Eq> {
  eq(lhs, rhs) {
    match (lhs, rhs) {
      (None, None) => True
      (Some(x), Some(y)) => x == y
      _ => False
    }
  }
}
