pragma { NoBuiltin }

import {
  boolean(...)
  cmp(...)
}

export data Option<a> {
  Some(a),
  None,
}

impl Eq Option<a: Eq> {
  eq(lhs, rhs) {
    match (lhs, rhs) {
      (None, None) => True
      (Some(x), Some(y)) => x == y
      _ => False
    }
  }
}
