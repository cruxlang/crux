import test(...)
import assert

test("number min and max", fun() {
  assert.equal(1 as Number, min(1, 2))
  assert.equal(2 as Number, max(1, 2))
})

test("number compare", fun() {
  assert.equal(LessThan, compare(1 as Number, 2))
  assert.equal(GreaterThan, compare(2 as Number, 1))
  assert.equal(EqualTo, compare(1 as Number, 1))
})
