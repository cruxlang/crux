data List<a> {
    Cons(a, List<a>),
    Nil,
}

let s = Cons(5, Cons(6, Cons(7, Nil)))

fun length(list) {
    match list {
        Nil => 0
        Cons(x, tail) => 1 + length(tail)
    }
}

let _ = print(length(s))
